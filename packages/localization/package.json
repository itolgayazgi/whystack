{
  "name": "@whystack/localization",
  "version": "0.1.0",
  "private": true,
  "description": "Application-language UI strings and the content-language / fallback model. Educational content and the terminology dictionary are NOT here — they live in content/.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "sideEffects": false,
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
