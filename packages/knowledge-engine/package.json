{
  "name": "@whystack/knowledge-engine",
  "version": "0.1.0",
  "private": true,
  "description": "The Topic model, the Knowledge Graph relationship types and the content validation rules. Pure logic: it reads no files and touches no database, because the React Native app imports it.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "sideEffects": false,
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
