{
  "name": "@whystack/api-client",
  "version": "0.1.0",
  "private": true,
  "description": "The API contract, once. Shared by the web application and the mobile client (ADR-0022): the single-flight refresh guard, the Problem Details mapping and every endpoint's shape live here, not twice.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "sideEffects": false,
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "dependencies": {
    "@whystack/localization": "workspace:*"
  }
}
