{
  "name": "@whystack/content-validation",
  "version": "0.1.0",
  "private": true,
  "description": "The gate content/ must pass. Loads every topic and the terminology dictionary from disk, and applies the rules in @whystack/knowledge-engine. Invalid content fails the build.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "content:validate": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@whystack/knowledge-engine": "workspace:*",
    "yaml": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
