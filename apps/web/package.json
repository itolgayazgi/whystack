{
  "name": "@whystack/web",
  "version": "0.1.0",
  "private": true,
  "description": "The website (ADR-0022). Landing, sign-in, registration, reading, progress — and the content studio. One codebase, one origin, one thing a person can call the site.",
  "scripts": {
    "dev": "next dev --turbopack -p 3000",
    "build": "next build",
    "start": "next start -p 3000",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@whystack/api-client": "workspace:*",
    "@whystack/localization": "workspace:*",
    "@whystack/markdown-renderer": "workspace:*",
    "@whystack/theme": "workspace:*",
    "next": "^15.5.4",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "@types/node": "^24.3.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "jsdom": "^29.1.1",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
